class Solution {
public:
    bool isPalindrome(int x) {
        double c=0,b=x;
        while(x>0)
        {
            c=c*10+x%10;
            x=x/10;
            
        }
        if(c==b)
            return true;
        else return false;
    }
};
